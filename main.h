#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h> // For using variable argument lists
#include <stddef.h> // For size_t, NULL, etc.

// Prototype for the custom _printf function
int _printf(const char *format, ...);

// Add prototypes for any additional helper functions you've written for your _printf function.
// For example, if you have a function to handle printing integers, add its prototype here.
// void print_int(int n);

#endif // MAIN_H